#ifndef DB_SETTINGS_H
#define DB_SETTINGS_H

const char *const UNI_DB_USERNAME = "db_reader";   // set your priviledge user if
                                                   // required (don't forget to
                                                   // recompile after changing)
const char *const UNI_DB_PASSWORD = "reader_pass"; // set your priviledge user if required (don't forget to
                                                   // recompile after changing)
const char *const UNI_DB_NAME = "UNIFIED_DB";      // option for DB adninistrator only
const char *const UNI_DB_HOST = "db-mpd.jinr.ru";  // option for DB adninistrator only

#endif // DB_SETTINGS_H
